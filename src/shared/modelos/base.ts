import { NON_BINDABLE_ATTR } from '@angular/compiler/src/render3/view/util'
import { EmailValidator } from '@angular/forms';
import { NgbPaginationNumber } from '@ng-bootstrap/ng-bootstrap'
import { isLabeledStatement, textSpanOverlap } from 'typescript';


export class Baner {
    string imagen;
    string label;
    string texto;
}

export class Baner {
    string nombre;
    string telefono;
    string direccion;
    string email;
    string bienvenida;
    baner[]: Baner;
}
